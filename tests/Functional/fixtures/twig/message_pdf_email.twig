{% autoescape %}
    {% set message = {
        'from': 'noreply@test.test',
        'subject': 'Test email with pdf',
        'attachments': [
            {
                'pdf': request.responses.0.content|raw,
                'filename': request.responses.0.filename|raw
            }
        ]
    } %}
{% endautoescape %}
{{ message|json_encode|raw }}